<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Superhero Squad Builder</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
<h1>Superhero Squad Builder</h1>
<div id="app">
    <div>
        <h2>Heroes</h2>
        <ul>
            {{#each heroes}}
                <li>{{name}} ({{specialPower}})</li>
            {{/each}}
        </ul>
    </div>
    <div>
        <h2>Squads</h2>
        <ul>
            {{#each squads}}
                <li>{{name}} ({{cause}})</li>
            {{/each}}
        </ul>
    </div>
</div>
<script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.7.7/handlebars.min.js"></script>
<script src="main"></script>

<script id="template" type="text/x-handlebars-template">
        <div>
            <h2>Heroes</h2>
            <ul>
                {{#each heroes}}
                <li>{{name}} ({{specialPower}})</li>
                {{/each}}
            </ul>
        </div>
        <div>
            <h2>Squads</h2>
            <ul>
                {{#each squads}}
                <li>{{name}} ({{cause}})</li>
                {{/each}}
            </ul>
        </div>
    </script>
</body>
</html>